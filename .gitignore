# build artifacts
/bin/
/dist/

# local SQLite state
*.db
*.db-wal
*.db-shm

# secrets — NEVER commit these
.env
.strava-auth-token
*.cookies
/cookies*.txt

# internal / non-public materials
# This repo is public — keep operational docs, deploy scripts, plans,
# and reverse-engineered API notes off the public tree. Originals live
# locally at ~/.local-docs/stravaKudos/.
/docs/
/deploy/
/plans/
